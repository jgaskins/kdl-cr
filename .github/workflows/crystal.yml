name: Crystal

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        crystal:
          - "1.13"
          - "1.14"
          - "1.15"
          - "1.16"
          - "1.17"
          - "1.18"
          - "1.19"
          - "1.20"
          - latest
          - nightly

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true

    - uses: crystal-lang/install-crystal@v1

    - run: shards install
    - run: crystal spec
